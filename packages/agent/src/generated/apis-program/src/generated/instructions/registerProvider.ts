/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findProviderPda } from "../pdas";
import { APIS_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const REGISTER_PROVIDER_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([254, 209, 54, 184, 46, 197, 109, 78]);

export function getRegisterProviderDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REGISTER_PROVIDER_DISCRIMINATOR
  );
}

export type RegisterProviderInstruction<
  TProgram extends string = typeof APIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountProvider extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountProvider extends string
        ? WritableAccount<TAccountProvider>
        : TAccountProvider,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RegisterProviderInstructionData = {
  discriminator: ReadonlyUint8Array;
  gpuSpecsHash: ReadonlyUint8Array;
  endpointUriHash: ReadonlyUint8Array;
};

export type RegisterProviderInstructionDataArgs = {
  gpuSpecsHash: ReadonlyUint8Array;
  endpointUriHash: ReadonlyUint8Array;
};

export function getRegisterProviderInstructionDataEncoder(): FixedSizeEncoder<RegisterProviderInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["gpuSpecsHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["endpointUriHash", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: REGISTER_PROVIDER_DISCRIMINATOR })
  );
}

export function getRegisterProviderInstructionDataDecoder(): FixedSizeDecoder<RegisterProviderInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["gpuSpecsHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["endpointUriHash", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getRegisterProviderInstructionDataCodec(): FixedSizeCodec<
  RegisterProviderInstructionDataArgs,
  RegisterProviderInstructionData
> {
  return combineCodec(
    getRegisterProviderInstructionDataEncoder(),
    getRegisterProviderInstructionDataDecoder()
  );
}

export type RegisterProviderAsyncInput<
  TAccountAuthority extends string = string,
  TAccountProvider extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Wallet that will own the new provider account. Pays rent. */
  authority: TransactionSigner<TAccountAuthority>;
  /** New Provider PDA. */
  provider?: Address<TAccountProvider>;
  systemProgram?: Address<TAccountSystemProgram>;
  gpuSpecsHash: RegisterProviderInstructionDataArgs["gpuSpecsHash"];
  endpointUriHash: RegisterProviderInstructionDataArgs["endpointUriHash"];
};

export async function getRegisterProviderInstructionAsync<
  TAccountAuthority extends string,
  TAccountProvider extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof APIS_PROGRAM_PROGRAM_ADDRESS,
>(
  input: RegisterProviderAsyncInput<
    TAccountAuthority,
    TAccountProvider,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  RegisterProviderInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountProvider,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? APIS_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    provider: { value: input.provider ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.provider.value) {
    accounts.provider.value = await findProviderPda({
      authority: getAddressFromResolvedInstructionAccount(
        "authority",
        accounts.authority.value
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("provider", accounts.provider),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRegisterProviderInstructionDataEncoder().encode(
      args as RegisterProviderInstructionDataArgs
    ),
    programAddress,
  } as RegisterProviderInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountProvider,
    TAccountSystemProgram
  >);
}

export type RegisterProviderInput<
  TAccountAuthority extends string = string,
  TAccountProvider extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Wallet that will own the new provider account. Pays rent. */
  authority: TransactionSigner<TAccountAuthority>;
  /** New Provider PDA. */
  provider: Address<TAccountProvider>;
  systemProgram?: Address<TAccountSystemProgram>;
  gpuSpecsHash: RegisterProviderInstructionDataArgs["gpuSpecsHash"];
  endpointUriHash: RegisterProviderInstructionDataArgs["endpointUriHash"];
};

export function getRegisterProviderInstruction<
  TAccountAuthority extends string,
  TAccountProvider extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof APIS_PROGRAM_PROGRAM_ADDRESS,
>(
  input: RegisterProviderInput<
    TAccountAuthority,
    TAccountProvider,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): RegisterProviderInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountProvider,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? APIS_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    provider: { value: input.provider ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("provider", accounts.provider),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRegisterProviderInstructionDataEncoder().encode(
      args as RegisterProviderInstructionDataArgs
    ),
    programAddress,
  } as RegisterProviderInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountProvider,
    TAccountSystemProgram
  >);
}

export type ParsedRegisterProviderInstruction<
  TProgram extends string = typeof APIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Wallet that will own the new provider account. Pays rent. */
    authority: TAccountMetas[0];
    /** New Provider PDA. */
    provider: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: RegisterProviderInstructionData;
};

export function parseRegisterProviderInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedRegisterProviderInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      provider: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRegisterProviderInstructionDataDecoder().decode(instruction.data),
  };
}
