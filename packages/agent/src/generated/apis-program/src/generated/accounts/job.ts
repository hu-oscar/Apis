/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getJobStatusDecoder,
  getJobStatusEncoder,
  type JobStatus,
  type JobStatusArgs,
} from "../types";

export const JOB_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  75, 124, 80, 203, 161, 180, 202, 80,
]);

export function getJobDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(JOB_DISCRIMINATOR);
}

export type Job = {
  discriminator: ReadonlyUint8Array;
  /** Buyer-supplied unique nonce so the same buyer can submit many jobs. */
  id: bigint;
  /** The wallet paying for the job. */
  buyer: Address;
  /** The provider PDA chosen for the job. */
  provider: Address;
  /** Price in USDC base units (6 decimals). 0 in W1 (no escrow). */
  priceLamportsUsdc: bigint;
  /**
   * sha256 of the full JobSpec JSON: prompt + model + scheduler + steps
   * + guidance_scale + seed + dtype + resolution + cuda/torch/diffusers
   * versions + gpu_arch_class. Per Research §4 this is what verification
   * reproduces — NOT just the prompt.
   */
  specHash: ReadonlyUint8Array;
  /**
   * Lifecycle state. W1 only ever sets `Created`. W2/W3 transition
   * through the remaining variants.
   */
  status: JobStatus;
  /**
   * Unix timestamp (seconds) at job creation. Tech Design §3 names this
   * `funded_at` because in W2 escrow is locked at the same instant.
   */
  fundedAt: bigint;
  /**
   * Unix timestamp (seconds) after which `auto_release` becomes valid
   * (W2). In W1 we still set this so the field is initialised.
   */
  deadline: bigint;
  /**
   * sha256 of the provider-signed completion artifact. `None` until the
   * provider calls `submit_completion` (W2).
   */
  completionProofHash: Option<ReadonlyUint8Array>;
  /** PDA bump. */
  bump: number;
};

export type JobArgs = {
  /** Buyer-supplied unique nonce so the same buyer can submit many jobs. */
  id: number | bigint;
  /** The wallet paying for the job. */
  buyer: Address;
  /** The provider PDA chosen for the job. */
  provider: Address;
  /** Price in USDC base units (6 decimals). 0 in W1 (no escrow). */
  priceLamportsUsdc: number | bigint;
  /**
   * sha256 of the full JobSpec JSON: prompt + model + scheduler + steps
   * + guidance_scale + seed + dtype + resolution + cuda/torch/diffusers
   * versions + gpu_arch_class. Per Research §4 this is what verification
   * reproduces — NOT just the prompt.
   */
  specHash: ReadonlyUint8Array;
  /**
   * Lifecycle state. W1 only ever sets `Created`. W2/W3 transition
   * through the remaining variants.
   */
  status: JobStatusArgs;
  /**
   * Unix timestamp (seconds) at job creation. Tech Design §3 names this
   * `funded_at` because in W2 escrow is locked at the same instant.
   */
  fundedAt: number | bigint;
  /**
   * Unix timestamp (seconds) after which `auto_release` becomes valid
   * (W2). In W1 we still set this so the field is initialised.
   */
  deadline: number | bigint;
  /**
   * sha256 of the provider-signed completion artifact. `None` until the
   * provider calls `submit_completion` (W2).
   */
  completionProofHash: OptionOrNullable<ReadonlyUint8Array>;
  /** PDA bump. */
  bump: number;
};

/** Gets the encoder for {@link JobArgs} account data. */
export function getJobEncoder(): Encoder<JobArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["id", getU64Encoder()],
      ["buyer", getAddressEncoder()],
      ["provider", getAddressEncoder()],
      ["priceLamportsUsdc", getU64Encoder()],
      ["specHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["status", getJobStatusEncoder()],
      ["fundedAt", getI64Encoder()],
      ["deadline", getI64Encoder()],
      [
        "completionProofHash",
        getOptionEncoder(fixEncoderSize(getBytesEncoder(), 32)),
      ],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: JOB_DISCRIMINATOR })
  );
}

/** Gets the decoder for {@link Job} account data. */
export function getJobDecoder(): Decoder<Job> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["id", getU64Decoder()],
    ["buyer", getAddressDecoder()],
    ["provider", getAddressDecoder()],
    ["priceLamportsUsdc", getU64Decoder()],
    ["specHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["status", getJobStatusDecoder()],
    ["fundedAt", getI64Decoder()],
    ["deadline", getI64Decoder()],
    [
      "completionProofHash",
      getOptionDecoder(fixDecoderSize(getBytesDecoder(), 32)),
    ],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link Job} account data. */
export function getJobCodec(): Codec<JobArgs, Job> {
  return combineCodec(getJobEncoder(), getJobDecoder());
}

export function decodeJob<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<Job, TAddress>;
export function decodeJob<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<Job, TAddress>;
export function decodeJob<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
): Account<Job, TAddress> | MaybeAccount<Job, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getJobDecoder()
  );
}

export async function fetchJob<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<Job, TAddress>> {
  const maybeAccount = await fetchMaybeJob(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeJob<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<Job, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeJob(maybeAccount);
}

export async function fetchAllJob(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<Job>[]> {
  const maybeAccounts = await fetchAllMaybeJob(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeJob(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<Job>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeJob(maybeAccount));
}
