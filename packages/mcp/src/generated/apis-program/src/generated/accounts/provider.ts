/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getProviderStatusDecoder,
  getProviderStatusEncoder,
  type ProviderStatus,
  type ProviderStatusArgs,
} from "../types";

export const PROVIDER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  164, 180, 71, 17, 75, 216, 80, 195,
]);

export function getProviderDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(PROVIDER_DISCRIMINATOR);
}

export type Provider = {
  discriminator: ReadonlyUint8Array;
  /** The wallet authorised to update this provider account. */
  authority: Address;
  /**
   * sha256 of the GPU spec JSON (model + VRAM + CUDA/driver versions).
   * We store the hash, not the raw spec, for size + privacy.
   */
  gpuSpecsHash: ReadonlyUint8Array;
  /**
   * sha256 of the worker websocket endpoint URI. Off-chain resolution
   * avoids leaking provider IPs publicly on-chain.
   */
  endpointUriHash: ReadonlyUint8Array;
  /**
   * SPL Token account holding the provider's bond.
   * `Pubkey::default()` in W1 (no bond yet); populated by `deposit_bond`
   * in W2.
   */
  bondVault: Address;
  /** Number of jobs currently `Funded` / `Started`. 0 in W1. */
  activeJobs: bigint;
  /** Lifetime jobs completed (cumulative). 0 in W1. */
  totalJobs: bigint;
  /** Operational status. New providers start at `Active` in W1. */
  status: ProviderStatus;
  /** Unix timestamp (seconds) when the provider account was created. */
  registeredAt: bigint;
  /** PDA bump. */
  bump: number;
};

export type ProviderArgs = {
  /** The wallet authorised to update this provider account. */
  authority: Address;
  /**
   * sha256 of the GPU spec JSON (model + VRAM + CUDA/driver versions).
   * We store the hash, not the raw spec, for size + privacy.
   */
  gpuSpecsHash: ReadonlyUint8Array;
  /**
   * sha256 of the worker websocket endpoint URI. Off-chain resolution
   * avoids leaking provider IPs publicly on-chain.
   */
  endpointUriHash: ReadonlyUint8Array;
  /**
   * SPL Token account holding the provider's bond.
   * `Pubkey::default()` in W1 (no bond yet); populated by `deposit_bond`
   * in W2.
   */
  bondVault: Address;
  /** Number of jobs currently `Funded` / `Started`. 0 in W1. */
  activeJobs: number | bigint;
  /** Lifetime jobs completed (cumulative). 0 in W1. */
  totalJobs: number | bigint;
  /** Operational status. New providers start at `Active` in W1. */
  status: ProviderStatusArgs;
  /** Unix timestamp (seconds) when the provider account was created. */
  registeredAt: number | bigint;
  /** PDA bump. */
  bump: number;
};

/** Gets the encoder for {@link ProviderArgs} account data. */
export function getProviderEncoder(): FixedSizeEncoder<ProviderArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["authority", getAddressEncoder()],
      ["gpuSpecsHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["endpointUriHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["bondVault", getAddressEncoder()],
      ["activeJobs", getU64Encoder()],
      ["totalJobs", getU64Encoder()],
      ["status", getProviderStatusEncoder()],
      ["registeredAt", getI64Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: PROVIDER_DISCRIMINATOR })
  );
}

/** Gets the decoder for {@link Provider} account data. */
export function getProviderDecoder(): FixedSizeDecoder<Provider> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["authority", getAddressDecoder()],
    ["gpuSpecsHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["endpointUriHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["bondVault", getAddressDecoder()],
    ["activeJobs", getU64Decoder()],
    ["totalJobs", getU64Decoder()],
    ["status", getProviderStatusDecoder()],
    ["registeredAt", getI64Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link Provider} account data. */
export function getProviderCodec(): FixedSizeCodec<ProviderArgs, Provider> {
  return combineCodec(getProviderEncoder(), getProviderDecoder());
}

export function decodeProvider<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<Provider, TAddress>;
export function decodeProvider<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<Provider, TAddress>;
export function decodeProvider<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
): Account<Provider, TAddress> | MaybeAccount<Provider, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getProviderDecoder()
  );
}

export async function fetchProvider<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<Provider, TAddress>> {
  const maybeAccount = await fetchMaybeProvider(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeProvider<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<Provider, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeProvider(maybeAccount);
}

export async function fetchAllProvider(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<Provider>[]> {
  const maybeAccounts = await fetchAllMaybeProvider(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeProvider(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<Provider>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeProvider(maybeAccount));
}

export function getProviderSize(): number {
  return 162;
}
