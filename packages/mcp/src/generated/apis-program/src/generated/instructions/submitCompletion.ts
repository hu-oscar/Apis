/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findProviderPda } from "../pdas";
import { APIS_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const SUBMIT_COMPLETION_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([155, 236, 211, 88, 53, 120, 74, 196]);

export function getSubmitCompletionDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SUBMIT_COMPLETION_DISCRIMINATOR
  );
}

export type SubmitCompletionInstruction<
  TProgram extends string = typeof APIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountProvider extends string | AccountMeta<string> = string,
  TAccountJob extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountProvider extends string
        ? ReadonlyAccount<TAccountProvider>
        : TAccountProvider,
      TAccountJob extends string ? WritableAccount<TAccountJob> : TAccountJob,
      ...TRemainingAccounts,
    ]
  >;

export type SubmitCompletionInstructionData = {
  discriminator: ReadonlyUint8Array;
  proofHash: ReadonlyUint8Array;
};

export type SubmitCompletionInstructionDataArgs = {
  proofHash: ReadonlyUint8Array;
};

export function getSubmitCompletionInstructionDataEncoder(): FixedSizeEncoder<SubmitCompletionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["proofHash", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: SUBMIT_COMPLETION_DISCRIMINATOR })
  );
}

export function getSubmitCompletionInstructionDataDecoder(): FixedSizeDecoder<SubmitCompletionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["proofHash", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getSubmitCompletionInstructionDataCodec(): FixedSizeCodec<
  SubmitCompletionInstructionDataArgs,
  SubmitCompletionInstructionData
> {
  return combineCodec(
    getSubmitCompletionInstructionDataEncoder(),
    getSubmitCompletionInstructionDataDecoder()
  );
}

export type SubmitCompletionAsyncInput<
  TAccountAuthority extends string = string,
  TAccountProvider extends string = string,
  TAccountJob extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  provider?: Address<TAccountProvider>;
  job: Address<TAccountJob>;
  proofHash: SubmitCompletionInstructionDataArgs["proofHash"];
};

export async function getSubmitCompletionInstructionAsync<
  TAccountAuthority extends string,
  TAccountProvider extends string,
  TAccountJob extends string,
  TProgramAddress extends Address = typeof APIS_PROGRAM_PROGRAM_ADDRESS,
>(
  input: SubmitCompletionAsyncInput<
    TAccountAuthority,
    TAccountProvider,
    TAccountJob
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  SubmitCompletionInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountProvider,
    TAccountJob
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? APIS_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    provider: { value: input.provider ?? null, isWritable: false },
    job: { value: input.job ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.provider.value) {
    accounts.provider.value = await findProviderPda({
      authority: getAddressFromResolvedInstructionAccount(
        "authority",
        accounts.authority.value
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("provider", accounts.provider),
      getAccountMeta("job", accounts.job),
    ],
    data: getSubmitCompletionInstructionDataEncoder().encode(
      args as SubmitCompletionInstructionDataArgs
    ),
    programAddress,
  } as SubmitCompletionInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountProvider,
    TAccountJob
  >);
}

export type SubmitCompletionInput<
  TAccountAuthority extends string = string,
  TAccountProvider extends string = string,
  TAccountJob extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  provider: Address<TAccountProvider>;
  job: Address<TAccountJob>;
  proofHash: SubmitCompletionInstructionDataArgs["proofHash"];
};

export function getSubmitCompletionInstruction<
  TAccountAuthority extends string,
  TAccountProvider extends string,
  TAccountJob extends string,
  TProgramAddress extends Address = typeof APIS_PROGRAM_PROGRAM_ADDRESS,
>(
  input: SubmitCompletionInput<
    TAccountAuthority,
    TAccountProvider,
    TAccountJob
  >,
  config?: { programAddress?: TProgramAddress }
): SubmitCompletionInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountProvider,
  TAccountJob
> {
  // Program address.
  const programAddress = config?.programAddress ?? APIS_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    provider: { value: input.provider ?? null, isWritable: false },
    job: { value: input.job ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("provider", accounts.provider),
      getAccountMeta("job", accounts.job),
    ],
    data: getSubmitCompletionInstructionDataEncoder().encode(
      args as SubmitCompletionInstructionDataArgs
    ),
    programAddress,
  } as SubmitCompletionInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountProvider,
    TAccountJob
  >);
}

export type ParsedSubmitCompletionInstruction<
  TProgram extends string = typeof APIS_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    provider: TAccountMetas[1];
    job: TAccountMetas[2];
  };
  data: SubmitCompletionInstructionData;
};

export function parseSubmitCompletionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSubmitCompletionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      provider: getNextAccount(),
      job: getNextAccount(),
    },
    data: getSubmitCompletionInstructionDataDecoder().decode(instruction.data),
  };
}
