{
  "name": "@apis/mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Apis MCP server — exposes the compute marketplace to AI agents over MCP (Streamable HTTP). x402 paywall lands in Sprint 4.4.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "echo \"mcp: no tests yet\" && exit 0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@solana/kit": "^6.9.0",
    "@solana/program-client-core": "^6.9.0",
    "@solana-program/token": "^0.13.0",
    "express": "^4.21.2",
    "pino": "^10.3.1",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.5",
    "tsx": "^4.21.0",
    "typescript": "~5.8.3"
  }
}
